//clear && cc tree.c -o tree -Wall -Werror && ./tree
#include <stdio.h>

struct binary_tree {
  int data;
  struct binary_tree* left;
  struct binary_tree* right;
};

typedef struct binary_tree node;

int main(int argc, char* argv[]){
  printf("hello, gushi ...\n");



  
  printf("bye, gushi!\n");
  return 0;
}
